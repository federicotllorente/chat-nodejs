import { hot } from 'react-hot-loader/root';
import React from 'react';

const Chat = () => {
    return (
        <h1>This is a chat, show all the messages in it</h1>
    );
};

export default hot(Chat);